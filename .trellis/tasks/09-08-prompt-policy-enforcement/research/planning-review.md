# 规划评审记录

日期：2026-09-08。范围：本任务的 PRD、技术设计与实施清单。结论仅表示规划一致性复核通过，不代表业务测试、模型效果、生产部署或用户实现授权。

## 已完成

两路独立只读评审分别检查了存储/恢复/兼容性，以及规则/覆盖/验收。初轮提出的问题在文档中修订后，由原评审方再次核对。

| 问题 | 最终契约 |
| --- | --- |
| 疑似请求也放行，可能被通过清理规则删除 | 只有 outcome=allowed 清除载荷，suspicious 同事务保留事件和 30 天证据 |
| 系统错误完成与后台恢复冲突 | request_outcome/response_intent 不可变，recovery_result 独立；原结果未提交则保持 unknown |
| 旧模式加密缺少密钥/发布前提 | 独立 evidence_protection，两步发布，覆盖全部 writer、后台 worker 和管理读取节点 |
| 历史同一 job 多事件可能串原文 | strict_capture 按 job 唯一，legacy_event 按 event 唯一，读取明确选择 origin |
| 原文读取与删除并发语义模糊 | 同一证据锁和授权事务，删除后不再授权新的读取，删除前在途授权不能撤回 |
| 线上观察阶段没有对应 strict 执行模式 | 本版采用同策略隔离重放后阻断试点，不增加 shadow、不用 legacy async 冒充 strict 效果 |
| 缺消息之外的工具文本字段 | 补 Chat/Responses/Claude/Gemini/媒体字段矩阵及正常文本掩盖工具风险的反例 |
| 总体召回可能掩盖单规则失效 | 规则与语言独立样本门槛，503/400/413 不算检出，另报端到端可用性 |

## 文档验证

- task.json 和两份上下文 JSONL 可解析，任务保持 planning，implementation_authorized=false。
- 上下文清单包含 8 条有效文件引用，无空占位条目。
- Markdown 本地链接、代码围栏、行尾空白、R01-R10 与 A01-A16 基础矩阵已检查；新增 A08b/A14b/A16b 用于修订后的关键反例。
- `git diff --check` 无输出；新增文档另行逐文件检查空白与引用。
- Trellis 官方脚本在本仓库缺失，未执行 task.py validate/start；没有声称官方验证通过。

## 尚未执行

没有修改业务代码、创建数据库 migration、调用模型、启用审核、连接生产或发送通知。P0 的政策条目登记、规则阈值/输入预算冻结和真实样本基线仍是后续实施任务；P1-P3 的代码、效果和运行验收均未执行。

用户尚未评审本次新设计。争议放行、30 天原文、故障拒绝及分期等为本版明确提案，不把文档复核当成用户批准。
