2026-09-19 独立只读复核完成，未发现剩余阻断。Dockerfile 提交 `d3737ab99` 将线上保留的发布工具编译并复制至 `/app/carpool-release`，与服务使用相同目标 GOOS/GOARCH、`CGO_ENABLED=0` 和构建依赖，保留可执行权限及 `sub2api` 所有权；服务默认入口、前端嵌入、版本参数，以及 CLI 的私有 manifest、禁止覆盖结果文件、发布门禁和迁移契约均未改变。Root 已另行完成 CLI Linux 编译和离线 `-h` 验证；完整多阶段生产镜像构建不在本次只读复核范围内。`runtime/lifecycle.py` 在任何停止/删除前核对固定资源名称白名单、任务 label、登记的容器/网络 ID、卷创建身份、网络端点和卷引用，拒绝外来关联；本次指出的两个边界已修复并再次读审确认：容器/网络 stop/rm 直接使用已登记的不可变 ID，优化模式 `python -O` 在生命周期模块导入时主动拒绝，避免跳过安全断言。Root 已验证正常 ownership check 通过、优化模式退出 1 符合预期。卷仍按精确名称与创建身份复核；无通配删除、prune、强制卷删除或递归文件清理，未标记匿名卷、镜像和 private 文件保持保留。本次未执行业务请求、stop 或 cleanup。
