# Sub2API 迁移完成记录

2026-09-10，Asia/Shanghai。服务于约 16:00 恢复，新机已承载真实请求。

## 当前入口

- 域名：`https://sub2api.monasapi.com`。
- 正式服务器：`155.138.234.216`；Cloudflare A 记录已更新，保持“仅 DNS”，TTL 自动（公网查询为 300 秒）。
- 新机目录：`/home/linuxuser/apps/sub2api`，活动槽位 `blue`。
- 旧机：`137.220.56.176`，SSH 别名 `newapi-vultr`；服务器和其他业务继续运行。
- 旧机 Sub2API router 已通过校验证书的 HTTPS 转发到新机。旧域名缓存及 MonasAPI 内部地址 `http://sub2api:8080` 均继续可用。
- 旧机 `sub2api-blue` 已正常停止，退出码 0，restart policy 为 `no`。旧数据库保留、不再承担生产写入。

## 数据与运行配置

已获用户确认：以旧机最新生产账务及配置为准；新机独有的 5 条管理员测试用量约 $1.572858504 留存备份，不并入正式账本。新机没有独有账号、代理或 API Key。

开放前，源库与目标库逐人/逐周期金额哈希及以下汇总全部相同：

| 项目 | 切换快照 |
|---|---:|
| 用户数 | 27 |
| 普通余额合计 | 18947.91442401 |
| 冻结余额合计 | 0 |
| 拼车周期数 | 22 |
| 拼车基础余额合计 | 6827.14538701 |
| 拼车加量余额合计 | 49.84107153 |
| 用量记录数 | 132011 |
| actual_cost 合计 | 20382.1941174800 |

金额是开放前快照，恢复后的正常请求会继续记账。按用户要求优先恢复服务，最终采用金额与用量校验，没有等待完整逐表内容比较。

两台应用镜像一致：`sha256:f51c7f038dbfdcc1bac4e70363abfc188a0d1bd9b19bcb86da45903993e2ed27`。新机运行环境变量已与源机逐项核对一致，包括 `CARPOOL_BACKGROUND_ENABLED=false`、`BATCH_IMAGE_QUEUE_ENABLED=false`。Redis 与业务文件随最终快照迁移。

## 验证

- 最终数据库与文件包 SHA-256 在传输前后相同；pg_restore 成功。
- 新机应用、router、PostgreSQL、Redis 均 healthy，应用重启次数 0。
- 新域名 HTTPS 和旧 IP 转发健康检查均为 200，证书校验成功。
- 管理后台页面 HTTP 200；未认证模型接口 401；带现有 API Key 的模型接口 200。
- `gpt-5.6-sol` 实际流式请求完成并返回 `OK`（约 7.53 秒）。
- 从旧机 MonasAPI 容器访问内部 Sub2API 地址正常。
- 公网 DNS 查询返回 `155.138.234.216`。
- 新机真实用量持续增长，旧机停留在 132011 条。
- 证书有效至 2026-11-29；certbot 模拟续期成功，自动续期及 Nginx reload hook 已就绪。
- 新机每 6 小时自动备份、每 30 秒只读健康检查已启用；首份备份与检查均成功。

## 备份与回退边界

旧机最终迁移资料：`/home/linuxuser/apps/sub2api/migration-20260910-1545/`。

新机迁移资料：`/root/sub2api-migration-20260910-1545/`，包含迁移前数据库/配置备份与旧机最终快照。

新机还完整保留：

- 数据库 `sub2api_pre_migration_20260910`；
- `/home/linuxuser/apps/sub2api/data.pre-migration-20260910`；
- `/home/linuxuser/apps/sub2api/redis_data.pre-migration-20260910`。

自动备份目录：`/home/linuxuser/apps/sub2api/backups/automated/`，首份为 `20260910T080732Z`。

**新机已经接受生产写入，回退不能只改 DNS 或直接启动旧应用。必须先暂停新请求、排空连接，把新机最新数据库/Redis/业务文件同步回旧机并核对金额，才能恢复旧机写入。**

原路由配置保存在旧机迁移资料目录 `router-before.conf`。不要在旧机直接运行常规部署/槽位切换脚本，否则可能重新启用过期数据库。

## 本机连接说明

排查期间，Clash 选中的香港节点访问源站异常；直接绑定物理网卡 `en1` 的 SSH/curl 可用。先在 Clash Verge 的活动 Merge、规则与运行配置中增加该域名 DIRECT、Fake-IP 排除及新机 IP /32 TUN 排除。随后确认 Chrome 实际使用 ClashX Pro 的系统代理 7890；该端口访问源站仍超时。已备份并在 ClashX 当前 iKuuu_V2 配置首条加入 `DOMAIN,sub2api.monasapi.com,DIRECT`，控制器热加载 204，显式经过 7890 访问首页 200（约 0.8 秒）。原先报错的 Chrome 标签页刷新后已显示海上列车首页及进入控制台。未更改全局节点、Codex 配置或登录方式。

系统代理绕过项的设置因需要管理员权限未成功，原设置保留。ClashX 规则已写入当前订阅文件，后续订阅更新可能覆盖，需要重新保留此域名规则。相关修改前备份位于本任务 work 目录。

接口排查另有独立发现：新机真实请求 `/v1/chat/completions`、`gpt-6-astra`，SSE HTTP 200 后返回 `upstream_error: Our servers are currently overloaded. Please try again later.`，没有完成输出。此前日志也有多个上游账号过载及重试，不能把 HTTP 200 一概当成生成成功。该接口问题尚未修复。


## 16:58 普通余额清理（迁移后另行授权）

用户明确要求：保留管理员普通余额，其他人的普通余额全部清零，拼车余额不动。

2026-09-10 16:58:32 已在新机数据库事务中完成：20 个原余额非零的非管理员账号清零，合计 9082.78071220；其余非管理员账号原本为 0。写入 20 条 `admin_balance` 调整记录，操作批次 `OBZ-20260910T165832`。事务内校验管理员余额及所有拼车周期的基础、加量、人工余额均未改变。提交后非管理员普通余额非零数为 0，合计为 0。已清理这些用户的普通余额缓存与 API 认证缓存，并发布缓存失效通知。

清零前逐用户备份：新机 `/root/sub2api-migration-20260910-1545/ordinary-balance-before-zero-20260910-165216.private.jsonl`；事务精确变更前值及结果：同目录 `ordinary-zero-20260910T165832.private.log`；本地任务 work 目录有对应副本。旧机迁移快照保留原状。

本次清零是迁移完成后的独立业务调整，不应再要求新旧服务器当前普通余额一致。

清零后验证：多位普通余额为 0 的非管理员用户继续产生拼车输出与用量记录（截至 17:00:30）。单独对用户 11 的最小请求未完整成功；17:00:20 的错误日志确认原因仍是上游账号 1 过载，不是余额不足。清零后查询未发现新增余额不足错误。
