# 0.2.6 兼容版本生产发布

2026-09-19 01:19:55（Asia/Shanghai）已恢复生产流量并完成验证。

- 地址：https://sub2api.monasapi.com
- 运行版本：`0.2.6-carpool-20260919`。
- 应用源码：`a8370c5529406e5333d818d95b95cde6bcc9d460`，已推送 `LiuShenXi/dd` 的 `main`；此后的发布记录提交仅修改文档与证据。
- 镜像：`sub2api:carpool-compatible-0.2.6-a8370c552`；生产 OCI manifest ID 为 `sha256:2d000b7bb658e8d1c95a4c2c676ccb6b9d6f9161631171ce76d6700a90f30f25`。
- 搬瓦工路径：`/home/linuxuser/apps/sub2api`；当前 blue 运行，原 green 停止并保留。

## 数据与服务验证

维护于 01:14:26 开始；现有 HTTP/WS 请求及待结算任务全部归零后锁定并停止旧应用。完成 PostgreSQL、Redis、应用数据及部署配置备份后才启动新版。

迁移由 291 条增至 297 条，原 291 条校验和及应用时间全部保留。升级前后全部拼车表、用户余额与配置、API Key 配置、显式额度、账号分组及用量计费汇总的受保护数据指纹一致；旧/新分组模型配置列一致。上游迁移仅按预期清理无限额度的全空限制记录。

最终直接端口、内部路由及公网均通过 `/health=200`、未认证 `/v1/models=401`。生产二进制版本/源码提交与镜像一致，公网版本及三个前端资源与新容器逐字节一致。发布门禁 open，容器重启次数 0。Codex 票据采集与注入保持默认关闭。

首次路由切换在 nginx reload 后立即读到了尚未退出的维护 worker，探针返回 503，切换脚本恢复维护页。确认新容器、迁移及数据指纹仍正确后，使用有界就绪重试重新切换；未重新迁移或恢复数据库。最终恢复时间如上，维护窗口约 5 分 29 秒。

## 验证与回滚材料

本地完整仓库集成测试 1500 通过、0 失败、1 个原有跳过；Linux/Windows lint 均为 0 问题。正式镜像在隔离数据库验证后才上传。详细历史验收见 [升级记录](UPGRADE_0_2_6.md) 和 [CI 验证记录](../validation/upgrade-026/evidence/release-ci-validation.md)。

脱敏部署回执见 [deployment-receipt.json](../validation/upgrade-026/evidence/deployment-receipt.json)；正式镜像验收见 [final-image-runtime-acceptance.json](../validation/upgrade-026/evidence/final-image-runtime-acceptance.json)；Docker config ID 与生产 OCI manifest ID 的等价证据见 [image-identity-equivalence.json](../validation/upgrade-026/evidence/image-identity-equivalence.json)。

生产私有备份保留在 `/home/linuxuser/apps/sub2api/releases/compatible-0.2.6-20260919`，未提交数据库、凭据或环境配置。原镜像和停止的 green 容器保留；远程原 main 保留在 `codex/backup-main-before-0.2.6-20260919`。旧二进制对 297 条迁移的兼容回退已在隔离环境验证。恢复生产计费后，不得用发布前数据库快照覆盖新增账务；应用回退仍须重新排空并核验就绪。
